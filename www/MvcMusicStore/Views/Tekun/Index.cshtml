@{
    ViewBag.Title = "Index";
}

<h2>@ViewBag.Message</h2>

@using (Html.BeginForm("PostToPaypal", "Tekun"))
{
<div>
    <table border="0" cellpadding="0" cellspacing="0">
        <tr>
            <td>
                Item Name:
            </td>
            <td>
                <input type="text" name="item" value="book" readonly />
            </td>
        </tr>
        <tr>
            <td>
                Price:
            </td>
            <td>
                RM<input type="text" name="amount" value="200" readonly />
            </td>
        </tr>
        <tr>
            <td>
            </td>
            <td>
                <input type="submit" name="btnsubmit" value="Pay with Paypal" />
            </td>
        </tr>
    </table>
</div>
}
@model IEnumerable<MvcMusicStore.Models.Genre>
@{
    ViewBag.Title = "Store";
}
<h3>Browse Genres</h3>

<p>
    Select from @Model.Count() genres:</p>
<ul>
    @foreach (var genre in Model)
    {
        <li>@Html.ActionLink(genre.Name, "Browse", new { genre = genre.Name })</li>
    }
</ul>
