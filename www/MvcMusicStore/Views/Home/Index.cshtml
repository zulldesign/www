@model List<MvcMusicStore.Models.Album>
@{
    ViewBag.Title = "ASP.NET MVC Music Store";
}
<div id="promotion">
</div>
@using (Html.BeginForm("PostToPaypal", "Home"))
{
<div>
    <table border="0" cellpadding="0" cellspacing="0">
        <tr>
            <td>
                Item Name:
            </td>
            <td>
                <input type="text" name="item" value="Reclaiming Satu Jalan Asphalt (RM150)" readonly />
            </td>
        </tr>
        <tr>
            <td>
                Price:
            </td>
            <td>
                RM<input type="text" name="amount" value="150" readonly />
            </td>
        </tr>
        <tr>
            <td>
            </td>
            <td>
                <input type="submit" name="btnsubmit" value="Pay with Paypal" />
            </td>
        </tr>
    </table>
</div>
}
<h3><em>Fresh</em> off the grill</h3>

<ul id="album-list">
    @foreach (var album in Model)
    {
        <li><a href="@Url.Action("Details", "Store",
                new { id = album.AlbumId })">

            <img alt="@album.Title" src="@album.AlbumArtUrl" />
            <span>@album.Title</span> </a>
        </li>
    }
</ul>