@model MvcMusicStore.Models.Cart

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Cart</legend>

    <div class="display-label">RecordId</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.RecordId)
    </div>

    <div class="display-label">Blog</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Blog.Title)
    </div>

    <div class="display-label">Count</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Count)
    </div>

    <div class="display-label">DateCreated</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.DateCreated)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
